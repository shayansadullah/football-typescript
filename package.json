{
  "name": "understanding-ts",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "lite-server",
    "tsconfig": "npx tsc -p tsconfig.json --noEmit -w",
    "prettier": "npx prettier . --write"
  },
  "author": "Maximilian Schwarzmüller",
  "license": "ISC",
  "devDependencies": {
    "lite-server": "^2.5.4",
    "prettier": "^3.4.2"
  },
  "dependencies": {
    "tsconfig": "^7.0.0"
  }
}
